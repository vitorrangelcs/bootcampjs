/**
 * Aritmeticos:
 * adição / concatenação: + (depende do tipo de dado, se for string com number ou string com string concatena, se for number com number soma)
 * potenciação: **
 * resto da divisao: %
 * divisao: /
 * multiplicação: *
 * 
 */


// importante, as contas são feitas como na matematica, ou seja, primeiro multiplicação e afins, depois soma ou subtrai

// o incremento pode ser feito com ++ antes ou depois do nome da variavel

// ++variavel ou variavel++


// operadores de decremento 
// --variavel ou variavel--

//not a number 

//normalmente acontece quando tentamos fazer uma conta que não é possivel

//ex 50* 'vitor' , isso vai dar NaN. 

// mas se fosse 50* '5' , o javascript ia tentar fazer, e até funcionaria